const mongoose = require("mongoose");
const User = require('../Model/User');


module.exports = {
    userRegistration:(req,res)=>{
        try{
        }
        catch{
            console.log('errror');
        }
    },
}